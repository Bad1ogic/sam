# Video Corruption Scanner (FFmpeg-based)
This is a lightweight Python script for scanning video files for frame-level corruption using FFmpeg. It detects frame decode errors, stream issues with full support for GPU acceleration fallback to CPU
